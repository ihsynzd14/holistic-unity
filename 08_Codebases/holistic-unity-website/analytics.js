/* ═══════════════════════════════════════════════════════════
   Holistic Unity — analytics.js — DEPRECATED stub
   ─────────────────────────────────────────────────────────────
   GA4 loading is handled by `shared.js`, which gates it behind
   the cookie consent banner (analytics category). Loading GA4
   here as well would (a) bypass consent, and (b) double-fire
   pageviews. This file is kept as an empty stub so the existing
   `<script async src="/analytics.js">` tags in 14 HTML pages
   don't 404. Remove the script tags + this file together when
   convenient.
   ═══════════════════════════════════════════════════════════ */
